{
  "loader": {
    "name": "java_plain",
    "version": "1.0"
  },
  "license": "${license}",
  "plugins": [
    {
      "id": "${name}",
      "entrypoint": "${group}.${name}.platforms.Sponge8Plugin",
      "name": "${project_name}",
      "description": "${project_description}",
      "version": "${version}",
      "branding": {},
      "links": {
        "homepage": "${homepage_url}",
        "source": "${repo_url}",
        "issues": "${issue_tracker_url}"
      },
      "dependencies": [
        {
          "id": "taterlib",
          "version": "${taterlib_version}",
          "load-order": "after",
          "optional": false
        }
      ]
    }
  ]
}
